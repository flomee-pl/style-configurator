spring:
  application:
    name: StyleConfigurator
  datasource:
    url: jdbc:postgresql://localhost:5432/styleconfigurator
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
